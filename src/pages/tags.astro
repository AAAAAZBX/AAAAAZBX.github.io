---
/**
 * 原 Tags 页已迁至 `/statistics`；保留此路由做 301，避免旧收藏与外链 404（含子路径部署 BASE_URL）
 */
const base = import.meta.env.BASE_URL;
const to =
  !base || base === "/"
    ? "/statistics"
    : base.endsWith("/")
      ? `${base}statistics`
      : `${base}/statistics`;
return Astro.redirect(to, 301);
---
