# 复制为 .env 并填写；Astro 中 PUBLIC_ 会注入到构建与浏览器端。

PUBLIC_WALINE_SERVER_URL=
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
# 配置 Supabase 后：在 SQL Editor 执行 `supabase/blog_visits.sql`（新库）与 `supabase/blog_visits_page_path.sql`（为 article 阅读量增加 page_path + 聚合视图，若表已建过可只跑后者）/statistics 页 Statistics 区块 M02 会按每篇文章的访问计条数。
# 访客画像 + /analytics 看板：执行 `supabase/blog_visit_intel.sql`（扩展 blog_visits 列与聚合视图；存储仍为 Supabase Postgres，无需 SQLite）。
# 可选：执行 `supabase/blog_visit_country_code.sql` 为 blog_visits / IP 缓存增加 country_code，Statistics 表格中国旗更准确；未执行时仍按国名推断部分国旗。

# 可选：高德开放平台「Web 服务」Key。填写后，国内公网 IP 的省/市/地图点优先用高德 v3，否则仍用 ipapi。
# 申请：https://console.amap.com/ → 应用管理 → 我的应用 → 添加「Web 服务」Key
# 安全：在 高德控制台 为该 Key 设置「域名白名单」，例如 https://你的github用户名.github.io 与 http://localhost:4321
PUBLIC_AMAP_WEB_KEY=
