# 复制为 .env 并填写；Astro 中 PUBLIC_ 会注入到构建与浏览器端。

PUBLIC_WALINE_SERVER_URL=
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
# 配置 Supabase 后：在 SQL Editor 执行 `supabase/blog_visits.sql`（新库）与 `supabase/blog_visits_page_path.sql`（为 article 阅读量增加 page_path + 聚合视图，若表已建过可只跑后者）/statistics 页 Statistics 区块 M02 会按每篇文章的访问计条数。
# 访客画像 + /analytics 看板：执行 `supabase/blog_visit_intel.sql`（扩展 blog_visits 列与聚合视图；存储仍为 Supabase Postgres，无需 SQLite）。
# 可选：执行 `supabase/blog_visit_country_code.sql` 为 blog_visits / IP 缓存增加 country_code，Statistics 表格中国旗更准确；未执行时仍按国名推断部分国旗。

# 非白名单访问邮件告警（可选）：纯静态站不能在浏览器里发信。在 Supabase 执行
# `supabase/blog_visit_whitelist_alert.sql`，部署 Edge Function `visit-ip-whitelist-alert`，
# 并在 Database Webhooks 对 blog_visits INSERT 指向该函数；密钥与 Resend 仅填在
# Dashboard → Edge Functions → Secrets，不要写进 .env 或仓库。
#
# 可选：高德开放平台「Web 服务」Key。填写后，国内公网 IP 的省/市/地图点优先用高德 v3，否则仍用 ipapi。
# 申请：https://console.amap.com/ → 应用管理 → 我的应用 → 添加「Web 服务」Key
# 安全：在 高德控制台 为该 Key 设置「域名白名单」，例如 https://你的github用户名.github.io 与 http://localhost:4321
PUBLIC_AMAP_WEB_KEY=

# Optional: security quant dashboard SQL.
# Run `supabase/blog_visit_security_dashboard.sql` after the base visit SQL files.
# It adds aggregate views for public analytics, private IP profile summaries,
# and `blog_visit_alert_events` for the whitelist email Edge Function.

# 内容可见性管理。用于 /admin?tab=access 保存时同步到 Supabase，
# 使公网部署站也能读取最新隐藏列表。在 Supabase 执行 supabase/content_visibility.sql 建表，
# 然后在 Supabase Dashboard → Project Settings → API → service_role 获取此 key。
SUPABASE_SERVICE_ROLE_KEY=

# 可选：在非 DEV 环境若仍存在可执行的 /api/visibility（例如误配 SSR），要求此密钥才允许用
# service_role 写入 Supabase。本地 astro dev 默认仍允许同步（不设也可）。
VISIBILITY_SYNC_SECRET=

# 内容可见性保存后自动触发 GitHub Pages 重新构建。
# GITHUB_TOKEN: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
# 需要 repo 权限。仓库 Settings → Secrets and variables → Actions → 确保不与此冲突。
GITHUB_TOKEN=
GITHUB_REPO=AAAAAZBX/AAAAAZBX.github.io

# --- AI 文章摘要（可选，构建时写入 src/generated/article-summaries.json）---
# 密钥放在 .env.local（已 gitignore），切勿提交仓库或粘贴到聊天。
# GitHub Actions：在仓库 Secrets 中配置同名变量。
#
# DeepSeek Anthropic 兼容示例：
# ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
# ANTHROPIC_MODEL=deepseek-v4-pro
#
ANTHROPIC_AUTH_TOKEN=
# 若工具使用另一变量名，脚本也认：
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=
ANTHROPIC_MODEL=
# 摘要输入正文最大字符数（默认 14000）；两次请求间隔毫秒（默认 600）
ARTICLE_SUMMARY_INPUT_CHARS=
ARTICLE_SUMMARY_DELAY_MS=
